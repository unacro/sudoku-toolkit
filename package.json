{
  "name": "@unacro/sudoku-toolkit",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run ./src/index.ts dev",
    "start": "bun run --watch ./src/index.ts dev",
    "test": "bun --watch test",
    "publish": "npm publish --access public"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
